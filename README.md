# semagerbot
```
 scrapy crawl keywords -s DEPTH_LIMIT=2 -a q=haus -a word_limit=2 -a pause_time=2
 scrapy crawl keywords -s DEPTH_LIMIT=2 -a q=haus,de_wohnung,huf -a word_limit=2 -a pause_time=2 -a prefix=xyz
 ```
